#include <stdio.h>
#include <stdlib.h>

int main() {
    int* x;
    if (x[10] == -1414812757) {
        printf("true: %d\n", x[10]);
    } else {
        printf("false: %d\n", x[10]);
    }

    /* int* buf = (int*) malloc(1000); */
    /* return buf[10]; */
    /* if (buf[5] == 0xAB) { */
    /*     return buf[0]; */
    /* } else { */
    /*     return 10; */
    /* } */
}
