#pragma once

// Minimal public runtime header.
// We intentionally keep this small first.
// Later we will add declarations here step by step.

namespace gensym_runtime {
  void runtime_link_test();
}
